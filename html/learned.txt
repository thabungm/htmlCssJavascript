- download git and install it
- vscode and install it
- create account on github.com
- clone the repository shared
- git steps
    -- Open Terminal by visiting the vscode menu
    -- git clone https://github.com/thabungm/htmlCssJavascript.git
    -- make changes & push it(Thabung)
    -- git pull origin main
- install live server
- emmet generates html snippet(inbuilt with vscode)    


Git quick commands
- To commit(Assuming that I am currently in the project folder)
    -- git add .
    -- git commit -m "Adding my changes"
    -- git push origin main
    


